#ifndef DISCORD_NOTIFY_H
#define DISCORD_NOTIFY_H

// Envoie une notification Discord via webhook
void send_discord_notification(const char *client_id, const char *hostname);

#endif // DISCORD_NOTIFY_H
